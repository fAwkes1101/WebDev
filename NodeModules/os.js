const os = require("os"); // This module is used to get details about operating systems

console.log(os.arch()); // 32 or 64 arch

console.log(os.networkInterfaces()); //about network you are on

console.log(os.platform()); //tells about palatform

console.log(os.cpus()); //about cpu